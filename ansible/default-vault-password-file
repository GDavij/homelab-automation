# Default Ansible Vault password file.
# Replace this value with a secure password before using in production.
# Usage example:
#   ansible-vault encrypt --vault-password-file default-vault-password-file <file>
#   ansible-playbook server_playbook.yml --vault-password-file default-vault-password-file

vault-temp-9gUd3T0bS
